import React from "react";
import Layout from "../UI/Layout";

const NotFound = () => {
    return (
        <Layout>
            <h1>404: Page Not Found</h1>
        </Layout>
    );
};

export default NotFound;
