import React from "react";
import Layout from "../components/UI/Layout";

const Register = () => {
    return (
        <Layout>
            <h1>Register</h1>
        </Layout>
    );
};

export default Register;
